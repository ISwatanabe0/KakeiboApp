import React from 'react';

const Analytics: React.FC = () => {
  return (
    <div className="p-4">
      <h1 className="text-2xl font-bold mb-4">分析・グラフ</h1>
      {/* 支出サマリー（今月・先月・年間） */}
      {/* グループ分析（比率・推移） */}
      {/* 推し分析（メンバー別・推し格差・推し変遷） */}
      {/* カテゴリ分析（内訳） */}
      {/* 現場分析（参戦回数・遠征費・接触回数） */}
    </div>
  );
};

export default Analytics;
